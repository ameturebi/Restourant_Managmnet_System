{
  "scripts": {
    "serve:customer": "npx http-server ./frontend/customer -p 8080",
    "vercel-build": "cd backend && npm install && npm run build"
  },
  "dependencies": {
    "@nestjs/jwt": "^10.2.0",
    "@nestjs/passport": "^10.0.3",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "wowjs": "^1.1.3",
    "express": "^4.18.2",
    "serverless-http": "^3.1.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.9",
    "@types/jquery": "^3.5.32"
  }
}
